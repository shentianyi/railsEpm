<%= form_for(@display_set_item) do |f| %>
  <% if @display_set_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@display_set_item.errors.count, "error") %> prohibited this display_set_item from being saved:</h2>

      <ul>
      <% @display_set_item.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :display_set_list %><br />
    <%= f.text_field :display_set_list %>
  </div>
  <div class="field">
    <%= f.label :department %><br />
    <%= f.text_field :department %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
