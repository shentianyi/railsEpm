<%= form_for(@kpiproperty) do |f| %>
  <% if @kpiproperty.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@kpiproperty.errors.count, "error") %> prohibited this kpiproperty from being saved:</h2>

      <ul>
      <% @kpiproperty.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
