<%= form_for(@kpi_property) do |f| %>
  <% if @kpi_property.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@kpi_property.errors.count, "error") %> prohibited this kpi_property from being saved:</h2>

      <ul>
      <% @kpi_property.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
