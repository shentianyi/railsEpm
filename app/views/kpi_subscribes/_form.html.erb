<%= form_for(@kpi_subscribe) do |f| %>
  <% if @kpi_subscribe.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@kpi_subscribe.errors.count, "error") %> prohibited this kpi_subscribe from being saved:</h2>

      <ul>
      <% @kpi_subscribe.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
